# Active brand

active:

> Chưa có brand nào. Chạy `/brand-onboard <URL hoặc tên>` để tạo brand đầu tiên,
> hoặc tạo thủ công `brands/<slug>/0_truth/` theo schema trong `_schema/`, rồi đặt `active: <slug>`.
